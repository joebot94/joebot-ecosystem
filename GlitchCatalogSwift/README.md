# GlitchCatalogSwift

SwiftUI shell rewrite of Glitch Catalog with `.jbt` JSON-backed local storage.

## Run

```bash
cd GlitchCatalogSwift
swift run
```

Registers as `glitch_catalog`, sends heartbeats every 5s, and disables Snapshot when Nexus is offline.
