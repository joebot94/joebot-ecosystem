# DirtyMixerApp

SwiftUI macOS control app for DirtyMixer hardware, connected through Nexus.

## Run

```bash
cd DirtyMixerApp
swift run
```

Registers as `dirtymixer_v1` and sends heartbeat/state updates through Nexus.
