# Observatory

SwiftUI monitor dashboard for Nexus-connected clients.

## Run

```bash
cd Observatory
swift run
```

Registers as `observatory` with type `monitor` and shows online/offline device cards.
