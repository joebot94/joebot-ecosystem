import SwiftUI

enum GlitchBoardTheme {
    static let background = Color(red: 0.10, green: 0.10, blue: 0.11)
    static let surface = Color(red: 0.15, green: 0.15, blue: 0.17)
    static let elevatedSurface = Color(red: 0.20, green: 0.20, blue: 0.22)
    static let accent = Color(red: 1.00, green: 0.40, blue: 0.00)
    static let gridMajor = Color.white.opacity(0.22)
    static let gridMinor = Color.white.opacity(0.09)
    static let waveform = Color.orange.opacity(0.92)
}
